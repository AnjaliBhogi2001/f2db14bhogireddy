extends layout

block content
  h1= title
  -
  - var result = results
  div.container-fluid
     div.row
        each item,index in result
            div.col-sm-2(onclick=`buy(${index})`) Dog Name: #{item["dogName"]}  <br>  Dog Age: #{item["dogAge"]} <br> Dog Breed: #{item["dogBreed"]} 
  script.
      function buy(index){
         alert("you purchased item "+(index+1))
      }
